# Code generated by servicegen. DO NOT EDIT.
# This overlay is the only project-level Compose configuration that mounts
# service source code. Use it through `make docker-up-dev`.
services:
  analyticsservice:
    image: analyticsservice-python-development:latest
    build: !reset null
    command:
      - /bin/sh
      - -lc
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345
          -m analytics_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        else
          exec python -m analytics_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        fi
    volumes:
      - ./analyticsservice:/workspace:ro
      - /workspace/.venv
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  automationservice:
    image: automationservice-python-development:latest
    build: !reset null
    command:
      - /bin/sh
      - -lc
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345
          -m automation_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        else
          exec python -m automation_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        fi
    volumes:
      - ./automationservice:/workspace:ro
      - /workspace/.venv
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  inventoryservice:
    image: inventoryservice-python-development:latest
    build: !reset null
    command:
      - /bin/sh
      - -lc
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345
          -m inventory_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        else
          exec python -m inventory_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        fi
    volumes:
      - ./inventoryservice:/workspace:ro
      - /workspace/.venv
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  orderservice:
    image: orderservice-python-development:latest
    build: !reset null
    command:
      - /bin/sh
      - -lc
      - >-
        if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345
          -m order_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        else
          exec python -m order_service.main
          --config /workspace/config/config.yaml
          --overrides /workspace/config/docker_overrides.yaml;
        fi
    volumes:
      - ./orderservice:/workspace:ro
      - /workspace/.venv
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE

volumes:
  go-dev-mod-cache:
  go-dev-build-cache:
  rust-dev-cargo-registry:
  rust-dev-cargo-git: