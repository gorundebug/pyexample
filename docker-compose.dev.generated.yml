# Code generated by servicegen. DO NOT EDIT.
# This overlay is the only project-level Compose configuration that mounts
# service source code. Use it through `make docker-up-dev`.
services:
  analyticsservice:
    image: analyticsservice-python-development:latest
    build: !reset null
    working_dir: /workspace/service
    command:
      - /bin/sh
      - -c
      - >-
        prepare-local-build /workspace/source /workspace/service
        && cd /workspace/service
        && if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345 -m analytics_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        else
          exec python -m analytics_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        fi
    environment:
      PYTHONPATH: /workspace/service/src
    volumes:
      - ./analyticsservice:/workspace/source:ro
      - python-dev-worktree-analyticsservice:/workspace/service
    ports:
      - "${ANALYTICS_SERVICE_DEBUG_PORT:-2345}:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  automationservice:
    image: automationservice-python-development:latest
    build: !reset null
    working_dir: /workspace/service
    command:
      - /bin/sh
      - -c
      - >-
        prepare-local-build /workspace/source /workspace/service
        && cd /workspace/service
        && if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345 -m automation_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        else
          exec python -m automation_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        fi
    environment:
      PYTHONPATH: /workspace/service/src
    volumes:
      - ./automationservice:/workspace/source:ro
      - python-dev-worktree-automationservice:/workspace/service
    ports:
      - "${AUTOMATION_SERVICE_DEBUG_PORT:-2346}:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  inventoryservice:
    image: inventoryservice-python-development:latest
    build: !reset null
    working_dir: /workspace/service
    command:
      - /bin/sh
      - -c
      - >-
        prepare-local-build /workspace/source /workspace/service
        && cd /workspace/service
        && if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345 -m inventory_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        else
          exec python -m inventory_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        fi
    environment:
      PYTHONPATH: /workspace/service/src
    volumes:
      - ./inventoryservice:/workspace/source:ro
      - python-dev-worktree-inventoryservice:/workspace/service
    ports:
      - "${INVENTORY_SERVICE_DEBUG_PORT:-2347}:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  orderservice:
    image: orderservice-python-development:latest
    build: !reset null
    working_dir: /workspace/service
    command:
      - /bin/sh
      - -c
      - >-
        prepare-local-build /workspace/source /workspace/service
        && cd /workspace/service
        && if [ "$${DEBUG:-0}" = "1" ]; then
          exec python -m debugpy --listen 0.0.0.0:2345 -m order_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        else
          exec python -m order_service.main --config ./config/config.yaml --overrides ./config/docker_overrides.yaml;
        fi
    environment:
      PYTHONPATH: /workspace/service/src
    volumes:
      - ./orderservice:/workspace/source:ro
      - python-dev-worktree-orderservice:/workspace/service
    ports:
      - "${ORDER_SERVICE_DEBUG_PORT:-2348}:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE

volumes:
  go-dev-mod-cache:
  go-dev-build-cache:
  rust-dev-cargo-registry:
  rust-dev-cargo-git:
  python-dev-worktree-analyticsservice:
  python-dev-worktree-automationservice:
  python-dev-worktree-inventoryservice:
  python-dev-worktree-orderservice: