[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "automation_service"
version = "0.2.58"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.9.2",
  "pyservicelib-gorundebug",
  "APScheduler==3.11.3",
  "temporalio==1.31.0",
  "model",
]
[tool.uv.sources]
pyservicelib-gorundebug = { git = "https://github.com/gorundebug/pyservicelib.git", tag = "v0.2.58" }
model = { workspace = true }

[dependency-groups]
dev = [
  "mypy>=1.11.0",
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
  "ruff>=0.6.0",
  "types-protobuf>=6.30.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/automation_service"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["automation_service"]

[[tool.mypy.overrides]]
module = ["*.generated.*"]
ignore_errors = true